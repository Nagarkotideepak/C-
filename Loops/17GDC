//GDC of Two Number
// Greatest Common Divisor
#include<iostream>
using namespace std;
int main(){
	int a,b;
	cout<<"Enter Two numbers: "<<endl;
	cin>>a>>b;
	while(a!=b){
		if(a>b)
			a=a-b;
		else if(b>a)
			b=b-a;
	}
	cout<<"GDC is : "<<a;
	return 0;
}
